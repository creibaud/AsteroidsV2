import core.settings.asteroid as asteroidSettings

def setRadius(radius):
    asteroidSettings.RADIUS = radius

def setSize(listSize):
    asteroidSettings.SIZE = listSize

def setMaxSpeed(maxSpeed):
    asteroidSettings.MAX_SPEED = maxSpeed

def setAnimationInterval(animationInterval):
    asteroidSettings.ANIMATION_INTERVAL = animationInterval