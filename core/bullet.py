import core.settings.bullet as bulletSettings

def setSpeed(speed):
    bulletSettings.BULLET_SPEED = speed

def setShootInterval(interval):
    bulletSettings.SHOOT_INTERVAL = interval

def setRadius(radius):
    bulletSettings.BULLET_RADIUS = radius