import core.settings.SpaceShip as spaceShipSettings

def setLenght(length):
    spaceShipSettings.LENGTH = length

def setWidth(width):
    spaceShipSettings.WIDTH = width

def setRotationSpeed(rotationSpeed):
    spaceShipSettings.ROTATION_SPEED = rotationSpeed

def setMaxSpeed(maxSpeed):
    spaceShipSettings.MAX_SPEED = maxSpeed

def setThrustPower(thrustPower):
    spaceShipSettings.THRUST_POWER = thrustPower

def setDeceleration(deceleration):
    spaceShipSettings.DECELERATION = deceleration